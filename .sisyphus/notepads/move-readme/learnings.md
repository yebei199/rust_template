## README.md moved to doc/README.md

- Used `git mv README.md doc/README.md` to move the file while preserving git history.
- Verified the move using `ls doc/README.md`.
- Confirmed the rename was staged for commit via `git status`.
